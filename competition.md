# JAKA Mini 2 机器人应用挑战赛 · 赛题细则(定稿版)

> 本版所有几何与时间参数均经实测校核(基于 `jaka_minicobo` URDF + MoveIt 2,
> 见 `design/规则校核报告.md`)。
> **赛场几何的唯一真源是 `jaka_competition_kit/config/arena.yaml`**;
> 改完任何尺寸,必须跑一遍 `ros2 run jaka_competition_kit arena_check` 校核。
> 本文件与 `arena.yaml` / `config/rules.yaml` 三者必须始终一致。

---

## 0. 通用约定(两条赛道共用)

### 0.1 赛场坐标系

以机械臂**基座安装面中心**为原点 `O`:

| 轴 | 方向 | 说明 |
|---|---|---|
| `+z` | 竖直向上 | 基座安装面 = 台面 = `z = 0` |
| `+y` | 从基座指向工位/散放区 | "正前方" |
| `+x` | 面向 `+y` 时的右侧 | "右侧" |

单位一律为 **mm**。

### 0.2 有效作业带(硬性约束)

**工具轴竖直向下时,可达作业带为:距基座水平距离 `180 ~ 420mm`。**

| 抓取高度 z | 竖直下抓可达半径 |
|---:|---|
| 30 mm | 180 – 420 mm |
| 120 mm | 155 – 406 mm |
| 200 mm | 85 – 380 mm |

**判据分两类**:

- **点目标**(工位中心、料盒中心):判这个点本身。料盒只要求**中心**在带内 ——
  工件就投在中心点上,料盒外缘可以超出作业带,机械臂从来不需要够到那里。
- **矩形区域**(散放区):必须判**最近点与最远角**。

- 只查四角是不够的 —— 近边中点的半径可能远小于近角。
  反例:350×250mm 的矩形若中心放在 `y=230`,四角是 204/397mm 看着合法,
  但**近边中点 `(0,105)` 只有 105mm**,落在基座回转体内部,`/compute_ik` 无解。
- 因此矩形区域的**最近点**(水平/竖直边向原点收拢得到的点)必须满足 `r ≥ 180mm`。

超出作业带后 `/compute_ik` 直接失败,比赛无法进行。若采用倾斜抓取(≤15°),
上限可放宽到 459mm,但边界附近可行构型极少、规划成功率低,**不建议**采用。

### 0.3 仿真赛与真机赛

| 项目 | 初赛(仿真) | 决赛(真机) |
|---|---|---|
| 平台 | ROS 2 Humble + MoveIt 2 假硬件(判分线) | ROS 2 Humble + JAKA Mini 2 真机 |
| 赛场定义 | `jaka_competition_kit/config/arena.yaml` | **同一份 `arena.yaml`** |
| 题目生成 | 官方场景生成器随机生成 | 裁判按生成器输出摆放 |
| 计时判分 | 自动计时 + 自动判分 | 自动计时 + 自动判分 + 裁判复核 |
| 提交物 | 一个 ROS 2 功能包 / 一个 `solve.py` | 同一份代码,现场部署 |

**一致性要求**:初赛与决赛使用**同一份 `arena.yaml`** 与**同一套评分程序**。
仿真赛成绩与真机赛成绩可直接比较。

> **判分只看 MoveIt 规划场景**(RViz 里那份), 它才是赛场几何的唯一真源。
> Gazebo 是**可选显示层**(`round.launch.py world:=gazebo`), 用来出转播画面,
> 不参与判分。详见 `docs/Gazebo仿真.md`。

### 0.4 编号规则

- 工位编号 **1–6**:靠近机械臂的第一排,从 `-x` 到 `+x` 为 1、2、3;
  第二排为 4、5、6。
- 二维码内容格式:`JAKA1-<6 位数字>`,例如 `JAKA1-246135`,
  表示抓取顺序 2→4→6→1→3→5。

---

## 赛道一:基础组 · 二维码序令定点抓取赛

### 1. 赛题定位

面向大一新生、高职高专院校、校赛初赛。考察**基础手眼标定、视觉识读与点位运动控制**。
无需识别物体类别,只需解析二维码中的抓取顺序,依次完成定点抓取。

### 2. 标准硬件配置

| 设备 | 规格 | 说明 |
|---|---|---|
| 机械臂 | JAKA Mini 2,6 轴 | 负载 2kg,臂展 580mm,重复定位精度 **±0.1mm** |
| 末端执行器 | 电动平行夹爪 | 行程 0–30mm,夹持力可调 |
| 视觉 | 单目工业相机 1280×720 | **眼在手外固定俯拍**,台面上方 700–900mm |
| 工位 | 6 个 100mm×100mm 标记板 | 位置固定,编号 1–6 |
| 工件 | 6 件规则几何体 | 正方体/长方体/圆柱/六角柱/三角柱/圆台,单重 ≤50g,工程塑料 |
| 指令载体 | 纸质二维码卡 60mm×60mm | 内容格式见 0.4 |
| 收纳料盒 | 200mm×150mm×40mm | 开口向上,需容纳 6 件工件 |

### 3. 赛场布局

```
                      俯视
 y(mm)
   ^
405|        +------+   +------+   +------+              |
   |        |  4   |   |  5   |   |  6   |  <- 远排     |
355|--------+------+---+------+---+------+--------------|
   |                                                   |
305|        +------+   +------+   +------+      +-----+|
   |        |  1   |   |  2   |   |  3   |      |料盒 ||
205|--------+------+---+------+---+------+      +-----+|
   |                       工位阵中心 y=280            |
110|                 +--------+                         |
   |                 | 二维码 |  <- 俯拍相机读取        |
 55|                 +--------+                         |
   |                          ###                      |
  0|--------------------------###--- JAKA Mini 2 ------+---> x(mm)
   |     -250      -150    0    +150      +250     +390
```

**精确坐标**

| 元素 | 中心坐标 (x, y) | 尺寸 | 距基座 r |
|---|---|---|---|
| 工位 1 | (-150, 205) | 100×100 | 254 mm |
| 工位 2 | (0, 205) | 100×100 | 205 mm |
| 工位 3 | (150, 205) | 100×100 | 254 mm |
| 工位 4 | (-150, 355) | 100×100 | 385 mm |
| 工位 5 | (0, 355) | 100×100 | 355 mm |
| 工位 6 | (150, 355) | 100×100 | 385 mm |
| 料盒 | (315, 180) | 200×150×40 | 363 mm(按中心判) |
| 二维码卡 | (0, 110) | 60×60 | 110 mm |

- 工位中心间距 **150mm**(100mm 工位 + 50mm 边缘间隙)。
- **两条赛道的料盒位置是重叠的**(赛道一 `(315,180)`,赛道二 `(310,200)`),
  因为两条赛道不会同时开赛。**场上只摆当前赛道的那一个料盒** ——
  同时在规划场景里放两个,另一个的侧墙会挡住工件入盒的路径。
- 二维码卡放在 `r = 110mm` 处,**俯拍相机读取即可,机械臂不需要移动过去**
  (该半径小于 180mm 的可达下限)。
- 工件初始时**置于各自工位中心**,底面贴台面。
- 料盒内腔 180mm×130mm。**不要做小**:150×100 的内腔(130×80)连
  6 件 50mm 工件都放不下,而且侧墙离工件只有 15mm,规划器末段稍有摆动
  就把工件怼到侧墙上(报 `Computed path is not valid`),表现为投放时好时坏。

### 4. 竞赛流程

**赛前准备(10 分钟)**:参赛队完成手眼标定、程序部署与调试,
可测试夹爪开合、点位精度与二维码读取。**调试期间的抓取不计成绩。**

**正式比赛(单轮限时 145 秒)**:

1. 裁判用生成器产生本轮二维码(`JAKA1-######`),替换场上的卡片;
2. 裁判发令,计时自动开始,参赛程序自动启动,**全程禁止人工干预**;
3. 程序读取二维码,解析 6 位抓取顺序;
4. 按顺序移动到对应工位,抓取工件并放入料盒;
5. 6 件全部入盒、或 145 秒倒计时结束、或触发安全终止,比赛结束。

**结束判定**:6 件全部入盒 / 145 秒超时 / 急停被按下 / 累计 3 次碰撞停机。

### 5. 评分规则(满分 100 分)

| 项目 | 分值 | 细则 |
|---|---:|---|
| 基础分 | 60 | 按二维码顺序每成功抓取并放入料盒 1 件得 10 分。**跳序、错序抓取的工件不计分**;该工件之后按二维码顺序继续。 |
| 效率分 | 40 | **105 秒内完赛得满分**;超过 105 秒后每 5 秒扣 4 分,扣完为止。 |
| 扣分 · 碰撞 | -5/次 | 机械臂碰撞工位标记板 / 台面 / 料盒,每次扣 5 分。 |
| 扣分 · 掉落 | -3/件 | 工件在搬运途中掉落至台面:该件不计分,另扣 3 分。 |
| 扣分 · 干预 | -10/次 | 人工干预一次扣 10 分;**累计 2 次成绩无效**。 |

**时间预算说明(基于参考实现实测)**:一件工件约 7 次运动(预抓取/下降/夹取/
抬起/去料盒/投放/退出),6 件共约 42 次。

官方参考实现 `ref_track1` 在 280mm 布局上实测:

| 轮次 | 成绩 | 用时 |
|---|---|---|
| 第 1 轮 | 6/6 | 99.7 秒 |
| 第 2 轮 | 6/6 | 102.7 秒 |
| 第 3 轮 | 6/6 | 102.8 秒 |
| 第 4 轮 | 6/6 | 112.8 秒 |
| **中位数** | **6/6** | **102.7 秒** |

**145 秒限时 = 实测中位数 × 1.4**(约 103 秒,给失败重试留 42 秒余量);
**105 秒效率满分线 = 实测中位数**,即"跑得和官方参考实现一样快就给满分"。
队伍要在效率分上拿分,就得在路径复用、减少无效规划、取消多余停歇上下功夫
——这正是本赛道效率分的考察点。基础分是**按件累计**的:即使 145 秒内只完成
4 件,也照样拿 40 分基础分,所以限时不会让慢队颗粒无收。

> **换机器必须重新标定**:`bash design/calibrate.sh 1 3`,取中位数 × 1.4 作为限时。
> 本节所有时间都是在**打开加速度限位**的 `joint_limits.yaml` 下测得的
> (见 `design/规则校核报告.md` 第 19 条);用仓库默认的关节限位会慢一倍。

### 6. 核心考察点

2D 手眼标定精度、二维码识读解析、点位运动控制、夹爪开合时序配合、路径效率优化。

---

## 赛道二:进阶组 · 泛化目标无序抓取赛

### 1. 赛题定位

面向本科院校、省赛 / 区域赛正赛。考察**泛化目标识别、抓取点生成与运动规划**。
不限制技术路线,鼓励 VLA、世界模型等前沿方案落地。

### 2. 标准硬件配置

| 设备 | 规格 | 说明 |
|---|---|---|
| 机械臂 | JAKA Mini 2 | 同赛道一 |
| 末端执行器 | 电动平行夹爪 | 同赛道一 |
| 视觉 | Intel RealSense D435i | 允许自备相机、接入大模型 API |
| 散放区 | 350mm × 160mm | 位置见 3. |
| 收纳料盒 | 200mm×150mm×100mm | |
| 工件库 | 20 种日用品 | 每轮随机抽取 **12 种**,单重 ≤200g |

### 3. 赛场布局

| 元素 | 中心坐标 (x, y) | 尺寸 | 说明 |
|---|---|---|---|
| 散放区 | (0, 270) | 350(x) × 160(y) | 工件中心须落在区内 |
| 料盒 | (310, 200) | 200×150×100 | |

散放区 `x ∈ [-175, 175]`,`y ∈ [190, 350]`,距基座:

| 判据点 | 半径 | 与作业带的关系 |
|---|---|---|
| 最近点 (0, 190) | **190 mm** | 距下界 180mm 有 10mm 余量 |
| 近角 (±175, 190) | 258 mm | 落在带内 |
| 最远角 (±175, 350) | **391 mm** | 距上界 420mm 有 29mm 余量 |

**为什么不是"350×250"**:矩形必须按**最近点**判可达性。250mm 的径向深度会让
近边中点掉进 180mm 以下的基座回转体,工件摆在那里机械臂够不到。
160mm 是本作业带内能放下、且能稳定塞进 12 件工件的最大径向深度
(已用 300 组随机种子验证:12 件全部放置成功)。

**关于预抓取高度**:工件越高,预抓取点越高(约 `工件高 + 81mm`),
可达半径随之收缩。散放区在 `z = 166mm` 处用 `/compute_ik` 实测
(见 `design/scatter_probe.py`)**整块可达**,这才是真正的瓶颈高度。

### 4. 标准工件库(20 种,每轮随机抽 12 种)

纸杯、半瓶矿泉水、钢卷尺、钥匙串、橡皮擦、有线鼠标、订书机、眼镜盒、
便签本、马克笔、胶带座、塑料螺丝刀、梳子、门禁卡套、橡皮圈、
小号扳手、茶杯垫、塑料夹子、名片盒、小号毛绒玩具。

**摆放规则**:12 件随机散放在散放区内,**互不重叠、无遮挡**;
任意两件的水平包围盒最小间隙 ≥10mm;工件长边与台面平行(不叠放)。

主裁判**直接照场景生成器的输出摆放**即可 —— 生成器已经保证 12 件互不重叠、
且在作业带内;屏幕会给出每件工件的中心坐标与尺寸,按坐标放即可,
不需要裁判自己凑。

### 5. 竞赛规则

**赛前准备(15 分钟)**:设备连接、算法部署与调试。

**正式比赛(单轮限时 320 秒)**:

1. 裁判按生成器输出摆放 12 件工件,发令后开始计时;
2. 程序自动识别全部工件,自主决定抓取顺序与抓取点;
3. 逐个抓取并放入料盒,**每次只能抓取 1 件**;
4. 抓取失败可重试,**单个工件最多重试 2 次**,超限则跳过;
5. 12 件全部入盒、或 320 秒倒计时结束、或触发安全终止,比赛结束。

**算法开放说明**:不限制技术路线。传统 2D 检测 + 3D 位姿估计、
点云分割、VLA 端到端模型、世界模型推理等均允许。

**时间预算说明(基于参考实现实测)**:官方参考实现 `ref_track2` 用最朴素的
"由近及远"顺序抓取(不做视觉、不挑抓取点),在 12 件散放上实测:

| 轮次 | 成绩 | 用时 |
|---|---|---|
| 第 1 轮 | 12/12 | 193.3 秒 |
| 第 2 轮 | 12/12 | 228.2 秒 |
| 第 3 轮 | 12/12 | 249.8 秒 |
| **中位数** | **12/12** | **228.2 秒** |

即**一件约 19 秒**。
**320 秒限时 = 实测中位数 × 1.4**(约 92 秒余量);
**230 秒效率满分线 = 实测中位数**。
也就是说:把顺序排好、把重复规划省掉,就能接近满分;
真正的差距来自识别与抓取点的稳定性(少失败、少重试)。

### 6. 评分规则(满分 150 分)

| 项目 | 分值 | 细则 |
|---|---:|---|
| 基础分 | 120 | 每成功抓取并放入料盒 1 件得 10 分,**满分为 12 件**。 |
| 效率分 | 30 | **230 秒内完赛得满分**;超过后按超时比例线性折算,到 320 秒为 0 分。 |
| 创新加分 | +5 ~ +20 | 采用 VLA、世界模型等端到端方案,无需预先标注工件模型即可完成抓取,经技术评定加分。 |
| 扣分 · 碰撞 | -5/次 | 碰撞台面 / 料盒每次扣 5 分。 |
| 扣分 · 多抓 | -10/次 | 单次夹取多件,每次扣 10 分。 |
| 扣分 · 干预 | -15/次 | 人工干预一次扣 15 分,**累计 2 次成绩无效**。 |

---

## 赛道对比

| 维度 | 赛道一(基础组) | 赛道二(进阶组) |
|---|---|---|
| 定位 | 入门实操考核 | 算法能力比拼 |
| 视觉 | 单目二维码识别 | 深度相机 / 自选方案 |
| 工件 | 规则几何体,固定位置 | 日用品,随机散放 |
| 难点 | 点位精度、顺序执行、效率 | 泛化识别、抓取点生成、避障规划 |
| 限时 | 145 秒 / 6 件 | 320 秒 / 12 件 |
| 满分 | 100 | 150 |
| 适配人群 | 低年级、高职 | 高年级、技术队 |

---

## 附录 A:判罚口径(裁判执行标准)

| 判罚项 | 判定方法 |
|---|---|
| 抓取顺序 | 评分系统记录每次抓取的工位 ID 与时间戳,自动比对二维码顺序 |
| 是否入盒 | 料盒称重(真机)/ 入盒事件(仿真)。工件需完全落入料盒、脱离夹爪 |
| 掉落 | 工件脱离夹爪后未进入料盒即判掉落 |
| 碰撞 | 真机:控制器报碰撞停机;仿真:规划器返回碰撞失败 |
| 单次多抓 | 夹爪闭合瞬间,夹持物水平投影面积 > 1.6 × 单件最大投影 |
| 互不重叠/无遮挡 | 摆放后俯拍一张,任意两件包围盒 IoU = 0 且最小间隙 ≥10mm,否则重摆 |
| 人工干预 | 触碰机械臂 / 工件 / 料盒 / 程序界面均计入;按下急停按"终止"处理,不计干预 |
| 急停 | 立即终止本轮,已入盒工件按基础分计,不计效率分 |

## 附录 B:安全要求

1. 比赛全程必须保留**独立于机械臂的物理急停按钮**,由裁判掌握。
2. 真机赛前必须完成 `set_payload`(按工件实际质量)与 `set_toolframe` 配置。
3. 真机最大速度限制:关节速度缩放不超过 50%;接近作业带边界时不超过 30%。
4. 机械臂运动范围内不得放置非比赛物品;选手席距机械臂 ≥800mm。
5. 触发碰撞停机后,须排除故障并复位后方可继续;累计 3 次碰撞直接终止本轮。

## 附录 D:参考实现实测(限时标定依据)

官方参考实现 `ref_track1` / `ref_track2` 在开发机上的实测结果。
**每换一次硬件都要重跑这张表**,限时按"中位数 × 1.4"重设。

| 项 | 赛道一 `ref_track1` | 赛道二 `ref_track2` |
|---|---|---|
| 策略 | 解码二维码 -> 按序抓取 | 由近及远顺序抓取(无视觉) |
| 成绩 | **6/6**(4 轮全中) | **12/12**(3 轮全中) |
| 用时样本 | 99.7 / 102.7 / 102.8 / 112.8 秒 | 193.3 / 228.2 / 249.8 秒 |
| 用手中位数 | **102.7 秒** | **228.2 秒** |
| 单件平均 | 约 17 秒 | 约 19 秒 |
| 运动次数 | 约 7 次/件 | 约 7 次/件 |
| 限时(×1.4) | **145 秒** | **320 秒** |
| 效率满分线(=中位数) | **105 秒** | **230 秒** |

> 上表是在**打开关节加速度限位**、`vel_scale=1.0`、MoveIt 假硬件 + RViz
> (本仓库开发机,RViz 常驻占用约 1.3 核)下测得的。关掉 RViz 通常会更快。
>
> ⚠️ **这组限时只对假硬件(判分)线成立, 不能拿到 Gazebo 线上用。** 同一套
> 参考实现在 Gazebo 真物理下跑赛道一要 209.4 秒(约 2 倍) —— 假硬件模式轨迹
> 是"瞬间到位", Gazebo 里关节得按真实速度走完、还要等物理收敛。若要把判分
> 搬到 Gazebo, 必须先用 `bash design/calibrate.sh` 在该线上重新标定。

标定命令:

```bash
# 赛前自检: 只规划不执行
ros2 launch jaka_competition_kit selfcheck.launch.py vel_scale:=0.3

# 计时标定: 跑一轮完整比赛, 成绩单在 ~/.ros/jaka_competition/
ros2 launch jaka_competition_kit round.launch.py track:=1 seed:=246135
```

## 附录 C:参数依据

| 参数 | 数值 | 来源 |
|---|---|---|
| 负载 | 2 kg | JAKA Mini 系列硬件手册 V03 Table 5-1 |
| 臂展 | 580 mm | 同上 |
| 重复定位精度 | **±0.1 mm** | 同上 |
| 关节范围 | J1/J4/J6 ±360°,J2 ±125°,J3 ±130°,J5 ±120° | 同上 |
| 有效作业带 | **半径 180–420 mm** | MoveIt 2 实测(`design/`) |
| URDF 关节 2 限位 | ±119.7°(比手册小 5.3°) | `jaka_minicobo.urdf` |
